#!/bin/sh -e

case $1 in
    configure)
        # Shutdown any running dmedia-service
        kill -s HUP `pidof -x dmedia-service` >/dev/null 2>&1 || true
        /usr/lib/dmedia/init-filestore /home
esac

#DEBHELPER#
